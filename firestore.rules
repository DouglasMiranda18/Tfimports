rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regras temporariamente permissivas para resolver problemas de acesso
    match /{document=**} {
      allow read, write: if true; // Acesso total temporário
    }
  }
}
